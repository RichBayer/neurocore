NeuroCore System Map

Root:
 /mnt/g/ai

├── backups
│   └── (system backups)

├── logs
│   └── (runtime logs)

├── memory
│   ├── chroma
│   │   ├── chroma.sqlite3        ← vector database
│   │   └── <uuid>/
│   │       ├── data_level0.bin
│   │       ├── header.bin
│   │       ├── length.bin
│   │       └── link_lists.bin
│   │
│   ├── knowledge
│   │   ├── home_infrastructure.md
│   │   └── linux_notes.txt
│   │
│   └── sessions
│       └── (session memory JSON files)

├── models
│   └── (managed by Ollama)

├── projects
│   └── neurocore
│       ├── build-logs
│       ├── docs
│       ├── runtime
│       ├── scripts
│       └── tools

├── runtime
│   └── python
│       └── jarvis-env
│           └── (Python virtual environment)

├── system
│   └── (system state + configs)

---

# Runtime Execution Flow

CLI (ai command)
    scripts/ai_cli.py
    - user interface
    - interactive + one-shot modes
    - streaming output

↓
UNIX Socket
    /tmp/neurocore.sock

↓
NeuroCore Daemon
    runtime/neurocore_daemon.py
    - persistent process
    - request normalization
    - streaming transport layer

↓
Runtime Manager
    runtime/runtime_manager.py
    - request routing
    - system coordination

↓
Control Plane
    runtime/control_plane.py
    - request classification
    - execution policy enforcement
    - routing authority

↓
┌───────────────────────────────┬───────────────────────────────┐
│                               │                               │
│        Execution Path         │        Reasoning Path          │
│                               │                               │
│  Execution Engine             │  Logic Layer (Router)          │
│  tools/execution_engine.py    │  scripts/jarvis_router.py      │
│                               │                               │
│  Tool                         │  Knowledge System              │
│  tools/system/service_manager │  scripts/query_knowledge.py    │
│                               │                               │
│  (Simulated Execution)        │  Vector Store (Chroma)         │
│                               │  /mnt/g/ai/memory/chroma      │
│                               │                               │
│                               │  Model Runtime (Ollama)        │
│                               │  http://localhost:11434        │
└───────────────────────────────┴───────────────────────────────┘

↓
Response Flow (Streaming)

    Tool / Model
    ↓
    Runtime Manager
    ↓
    Daemon
    ↓
    CLI (real-time output)

---

# System Behavior

Cold Start:
    - embeddings + vector DB initialize
    - slower first reasoning query

Warm State:
    - no reinitialization
    - fast streaming responses

Execution Path:
    - no model required
    - deterministic
    - immediate response

---

# System Properties

- persistent daemon
- stateless CLI
- lazy-loaded knowledge system
- real-time streaming pipeline
- control plane governed execution
- tool-based execution system
- confirmation-based execution safety model

---

# Current Capabilities

- semantic retrieval (RAG)
- streaming LLM responses
- interactive CLI
- persistent runtime
- execution engine
- tool registry
- structured tool interface
- controlled execution (confirmation required)

---

# Current Limitations

- execution is simulated only
- no real system command execution yet
- no observability / logging layer
- no task persistence
- no long-term memory
- no session-aware confirmation

---

# Next Phase

- real system execution (systemctl integration)
- policy expansion
- observability layer
- execution logging
- task persistence